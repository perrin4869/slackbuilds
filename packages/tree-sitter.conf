CATEGORY=libraries
PRGNAM=tree-sitter
SOURCE=github
GITHUB_REPO=tree-sitter/tree-sitter
TAG_REGEX='^v([0-9]+\.[0-9]+\.[0-9]+)$'
GENERATOR=simple
SRC_URL="https://github.com/tree-sitter/tree-sitter/archive/v%VERSION%/tree-sitter-%VERSION%.tar.gz"
ARCHIVE="tree-sitter-%VERSION%.tar.gz"
PRGDIR="tree-sitter-%VERSION%"

# Last version this repo opened an update PR for. Dedupe only - the
# authoritative "what is currently shipped" is always read from
# SlackBuildsOrg/slackbuilds@master at detection/submit time.
VERSION=0.26.11
